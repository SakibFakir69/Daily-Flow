/**
 * Web stub for the native-ad card. Native ads have no web support, so render
 * nothing. Metro picks this over ./ad-card.tsx when bundling for web.
 */
export function AdCard() {
  return null;
}
